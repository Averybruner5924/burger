<h1>Burgers!</h1>

<h2>Burgers that have not been devored!</h2>

<ul>
  {{#each burgers}}
    {{#unless devor}}
      {{> burgers/burger-block devor=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Hungry!</h2>

<ul>
  {{#each burgers}}
    {{#if devor}}
      {{> burgers/burgers-block devor=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Devor</label><br>
    <input type="radio" name="sleepy" value="1" checked> Devor!!<br>
  </div>

  <button type="submit">Add Burger</button>
</form>
